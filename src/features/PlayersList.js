const PlayersList = ({ listPlayers }) => {
    console.log('list', listPlayers);
    return (
        <>
            <h1> You are seeing the PlayersList component</h1>
            <h4> check console to see list of players</h4>
        </>
    )
};

export default PlayersList;